import 'package:flutter/cupertino.dart';

class SalesVoucherPage extends StatelessWidget {
  const SalesVoucherPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text('Sale Voucher'),
      ],
    );
  }
}
